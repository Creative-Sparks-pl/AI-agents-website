---
version: v3.6
date: "2026-03-31"
title: Full mapping process with screenshots
description: |
  Existing-site mapping now runs as a full process, with screenshots as the visual reference.
changes:
  - "Full process in 5 phases: subpage inventory, screenshots, structure mapping, context and strategy, wireframes. Three checkpoints where you can approve progress"
  - Automatic full-page screenshots with cookie banners dismissed. If the screenshot tool is missing, the agent walks you through the install (globally / scoped to the agent / skip)
  - Every page template is processed in parallel, not sequentially. You get results faster on larger sites
  - "Every subpage is described in 6 fields per section: type, purpose, user question, layout, elements, copy from the original site"
  - Context and UX strategy are filled in automatically from the site analysis, not from a conversation with the user
  - "Self-check: after the wireframe is built, the agent verifies that every section and element from the map made it into the final HTML"
---
